<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/item_order"
        android:title="Добавить продукт"
        android:icon="@drawable/ic_add"/>

    <item
        android:id="@+id/item_cached"
        android:title="Восстановить продукт"
        android:icon="@drawable/ic_cached"/>

    <group android:checkableBehavior="single">

        <item android:id="@+id/item_help"
            android:title="Помощь"
            android:icon="@drawable/ic_error"/>
    </group>

    <group android:checkableBehavior="single">

        <item android:id="@+id/item_exit"
            android:title="Выйти"
            android:icon="@drawable/ic_close"/>
    </group>

</menu>